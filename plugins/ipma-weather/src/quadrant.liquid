{% if lang == "pt" %}
  {% assign label_today = "HOJE" %}
{% else %}
  {% assign label_today = "TODAY" %}
{% endif %}

{% assign tomorrow = data[1] %}

<div class="layout layout--row layout--center gap--xsmall lg:gap--small">
  <div class="flex flex--col flex--center gap--xsmall grow">
    <span class="label label--small">{{ label_today }}</span>
    <span class="label label--small hidden lg:block">{{ display_name }}</span>

    <img
      class="image image--adaptive w--[11cqw] h--[11cqw] lg:w--[14cqw] lg:h--[14cqw]"
      src="{{ icon_url }}"
      alt="{{ weather }}"
    >

    <span class="value value--medium lg:value--large value--tnums" data-fit-value="true">{{ today.tMax | round }}°</span>
    <span class="label label--small text--center">{{ weather }}</span>
    <span class="label label--small">{{ label_min }} {{ today.tMin | round }}°</span>
    <span class="label label--small hidden lg:block text--center">
      {{ label_rain }} {{ today.precipitaProb | round }}% · {{ today.predWindDir }} · {{ wind_speed }}
    </span>
  </div>

  <div class="divider--v"></div>

  <div class="flex flex--col flex--center gap--xsmall grow">
    <span class="label label--small">{{ tomorrow.forecastDate | date: "%a" | capitalize }}</span>

    {% render "day_icon_img", day: tomorrow, icon_class: "w--[9cqw] h--[9cqw] lg:w--[12cqw] lg:h--[12cqw]" %}

    <span class="value value--medium lg:value--large value--tnums" data-fit-value="true">{{ tomorrow.tMax | round }}°</span>
    <span class="label label--small value--tnums">{{ tomorrow.tMin | round }}°</span>
    <span class="label label--small hidden lg:block">{{ label_rain }} {{ tomorrow.precipitaProb | round }}%</span>
  </div>
</div>

{% render "title_bar", display_name: display_name %}